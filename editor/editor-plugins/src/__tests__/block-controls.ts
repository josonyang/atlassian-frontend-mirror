// THIS FILE IS GENERATED. DO NOT MODIFY IT MANUALLY.
export {};

describe('block-controls wrapper', () => {
	it('check ./block-controls exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls');
		const wrapper = require('../block-controls/index');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils/anchor-name exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils/anchor-name');
		const wrapper = require('../block-controls/entry-points/block-decoration-utils-anchor-name');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils/consts exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils/consts');
		const wrapper = require('../block-controls/entry-points/block-decoration-utils-consts');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils/dom-attr-name exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils/dom-attr-name');
		const wrapper = require('../block-controls/entry-points/block-decoration-utils-dom-attr-name');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils/drag-handle-positions exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils/drag-handle-positions');
		const wrapper = require('../block-controls/entry-points/block-decoration-utils-drag-handle-positions');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils/visibility-container exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils/visibility-container');
		const wrapper = require('../block-controls/entry-points/block-decoration-utils-visibility-container');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils/widget-positions exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils/widget-positions');
		const wrapper = require('../block-controls/entry-points/block-decoration-utils-widget-positions');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/block-decoration-utils exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/block-decoration-utils');
		const wrapper = require('../block-controls/ui/block-decoration-utils');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/blockControlsPlugin exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/blockControlsPlugin');
		const wrapper = require('../block-controls/entry-points/blockControlsPlugin');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});

	it('check ./block-controls/blockControlsPluginType exports all the same variables as the original', () => {
		const original = require('@atlaskit/editor-plugin-block-controls/blockControlsPluginType');
		const wrapper = require('../block-controls/entry-points/blockControlsPluginType');
		const originalKeys = Object.keys(original).sort();
		const wrapperKeys = Object.keys(wrapper).sort();
		expect(originalKeys).toEqual(wrapperKeys);
	});
});
